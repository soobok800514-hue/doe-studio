# 🚀 DOE Studio 빠른 시작 가이드 (신입사원용)

> 한 페이지로 끝내는 첫 사용 설명서. 출력해서 책상 옆에 두세요.

---

## 30초 요약

```
"인자 입력 → 시험 진행 → 결과 업로드 → 최적 조건 자동 도출"
```

기존에 Minitab으로 하던 작업을 **웹 브라우저에서 한국어 UI**로 동일하게 처리합니다.

---

## 1단계 ☑ 설치 (최초 1회, 약 5분)

```bash
# 1) 프로젝트 받기
git clone <사내-Git-URL>/doe_studio.git
cd doe_studio

# 2) 가상환경 (선택이지만 권장)
python -m venv venv
source venv/bin/activate    # Windows: venv\Scripts\activate

# 3) 패키지 설치
pip install -r requirements.txt
```

## 2단계 ☑ 실행

```bash
streamlit run app.py
```

→ 브라우저에 `http://localhost:8501` 열림. 자동으로 안 열리면 직접 접속.

---

## 3단계 ☑ 첫 분석 (예제로 10분 만에 체험)

| 순서 | 작업 | 페이지 |
|---|---|---|
| 1 | 개념 훑어보기 | 📘 사용법 |
| 2 | "시트벨트 PT/LL (L9)" 예제 로드 | 📁 예제 데이터 |
| 3 | "이 예제로 분석 시작" 클릭 | 📁 예제 데이터 |
| 4 | 인자/응답 컬럼 선택 → 결과 자동 출력 | 📊 Taguchi 분석 |
| 5 | "최적화 실행" 버튼 클릭 | 🎯 반응최적화 |

---

## 4단계 ☑ 실제 시험에 적용

### A. 설계표 만들기
1. **🔬 Taguchi 설계** 페이지로 이동
2. 인자 수 입력 (예: 3)
3. 각 인자별 이름·수준값·단위 입력
4. **추천 OA 자동 표시** 확인 후 [설계표 생성] 클릭
5. CSV 다운로드

### B. 시험 진행
- CSV의 `Run_Order` 순서대로 실험 실시
- 결과를 **응답 컬럼**에 입력 (예: `Response_Y`, `MaxLoad` 등)

### C. 분석
1. **📊 Taguchi 분석** 페이지에서 CSV 업로드
2. S/N 비 유형 선택 (Larger / Smaller / Nominal)
3. 자동으로 출력되는 항목:
   - 평균/S/N 응답표 (Delta, Rank)
   - 주효과도
   - ANOVA (기여율, p-value)
   - 최적 수준 추천

### D. 다중 응답 최적화 (응답이 2개 이상일 때)
1. **🎯 반응최적화** 페이지로 이동
2. 응답별 사양 입력:
   - 🔼 최대화: L(최소), T(목표)
   - 🔽 최소화: T(목표), U(최대)
   - 🎯 목표값: L, T, U 모두
3. [최적화 실행] → 자동으로 최적 인자 조건 도출

---

## 5단계 ☑ 확인 실험

발견한 최적 조건으로 **1~3회 추가 시험**:
- 예측 응답값과 실측치를 비교
- 차이가 크면 → 상호작용이나 모형 가정 검토 (사내 슬랙 `#doe-studio` 채널 문의)

---

## ⚠️ 반드시 알아둘 점

| 상황 | 어떻게? |
|---|---|
| **공식 GMP/ISO 보고서가 필요** | Minitab 결과를 정본으로 유지하세요. 본 도구는 보조용. |
| **CSV의 컬럼명을 바꿔야 함** | 가능하지만 분석 페이지에서 다시 매칭해야 함. 권장 X |
| **한글 깨짐** | 다운로드 CSV는 `UTF-8 BOM` 인코딩. Excel에서 정상 표시됨. |
| **상호작용 분석 필요** | v0.1은 주효과만 지원. v0.2 로드맵에 포함 예정. |
| **L32 이상 필요** | 현재 미지원. Minitab으로 처리 후 결과만 본 도구에서 시각화. |

---

## 💡 자주 하는 실수 Top 3

1. **단위 혼동** : LoadCell 데이터를 kN으로 입력했는데 사양은 N으로 설정 → 결과 엉뚱하게 나옴
2. **무작위 순서 무시** : 다운로드한 CSV의 `Run_Order`를 무시하고 `Std_Order`로 시험 진행 → 잡음 편향 발생
3. **반복 없이 Nominal S/N 시도** : Nominal-the-best는 같은 조건 2회 이상 측정 필요

---

## 🆘 도움이 필요할 때

- 사내 슬랙: **`#doe-studio`** 채널
- 매주 화요일 오전 10시: 신입사원 DOE Studio 워크숍 (선착순 5명)
- Minitab과의 결과 검증: 분기별 자동 검증 리포트 발송 예정

---

**마지막 팁**: 처음에는 무조건 **📁 예제 데이터**부터 시작하세요. 워크플로우 한 번 돌려보면 80%는 이해됩니다.
